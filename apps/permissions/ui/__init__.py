"""
Role & Permission Management UI.

Dashboard pages for managing roles, permissions, store memberships, and
viewing audit logs. This sub-package is a UI layer that sits on top of
the core RBAC primitives in ``apps.permissions``.

Supports both superuser (cross-store) and store-admin (single-store
scope) workflows. See ``apps/permissions/ui/README.md`` for details.
"""
